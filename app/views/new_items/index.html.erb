<h1>Listing new_items</h1>

<table>
  <thead>
    <tr>
      <th>User</th>
      <th>Title</th>
      <th>Url</th>
      <th>Content</th>
      <th>Votes</th>
      <th>Flagged</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @new_items.each do |new_item| %>
      <tr>
        <td><%= new_item.user %></td>
        <td><%= new_item.title %></td>
        <td><%= new_item.url %></td>
        <td><%= new_item.content %></td>
        <td><%= new_item.votes %></td>
        <td><%= new_item.flagged %></td>
        <td><%= link_to 'Show', new_item %></td>
        <td><%= link_to 'Edit', edit_new_item_path(new_item) %></td>
        <td><%= link_to 'Destroy', new_item, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New New item', new_new_item_path %>
